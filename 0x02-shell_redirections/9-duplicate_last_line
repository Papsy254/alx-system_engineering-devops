#!/bin/bash
# Read the last non-empty line of the file
last_line=$(grep . iacta | tail -n 1)

# Append the last non-empty line to the file
if [ -n "$last_line" ]; then
    echo "$last_line" >> iacta
fi
